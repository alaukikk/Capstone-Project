
# Question --> Fetch relevant ref. material --> Feed question and material to asmall model ---> Model answers from material rather than memory

from typing import Optional


def try_rag_small_model(request_text: str) -> Optional[str]:
    """Returns None until real retrieval + small-model generation is wired up."""
    return None
