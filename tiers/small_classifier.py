
# for requests that need some judgment or categorization, but not the full power/cost of AI

from typing import Optional


def try_small_classifier(request_text: str) -> Optional[str]:
    """Returns None until a real small classifier model is wired up."""
    return None
